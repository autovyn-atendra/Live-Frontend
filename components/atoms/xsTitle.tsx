const XsTitle = ({ text }) => {
  return (
    <h6 className="text-xs mt-2 mb-2 font-bold uppercase focus:ring w-full ease-linear transition-all duration-150">
      {text}
    </h6>
  );
};

export default XsTitle;
