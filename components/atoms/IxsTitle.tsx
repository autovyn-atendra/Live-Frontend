const IXsTitle = ({ text }) => {
  return (
    <h6 className="text-xs mt-1 mb-2 font-bold focus:ring w-full ease-linear transition-all duration-150">
      {text}
    </h6>
  );
};

export default IXsTitle;
